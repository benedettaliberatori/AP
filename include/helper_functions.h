//#include <BST.h>
#include "iterators.h"

#include <vector>
#include <algorithm>


//// helper functions for BST class
//
//
//// my_find
//template<typename k_t, typename v_t, typename OP> 
//std::pair<typename bst<k_t, v_t, OP>::iterator, const bool> my_find(const k_t& x);
//
//
//// find
//template<typename k_t, typename v_t, typename OP> 
//typename bst<k_t, v_t, OP>::iterator find (const k_t& x);
////const std::pair<const_iterator, const bool> my_find(const k_t& x) const;
//
//// find const
//template<typename k_t, typename v_t, typename OP> 
//typename bst<k_t, v_t, OP>::const_iterator find (const k_t& x);
//
//
//// _insert
//template<typename k_t, typename v_t, typename OP> template<typename O> std::pair<typename bst<k_t, v_t, OP>::iterator,bool>  _insert(O&& x);
//                                                                                                                                                                                                                                                                                                                                                                                                           
//template<typename k_t, typename v_t, typename OP> 
//std::pair<typename bst<k_t, v_t, OP>::iterator,bool>  insert(const std::pair<const k_t, v_t>& x);
//
//template<typename k_t, typename v_t, typename OP> 
//std::pair<typename bst<k_t, v_t, OP>::iterator,bool>  insert(std::pair<const k_t, v_t>&& x);        











